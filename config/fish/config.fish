# Set fish user abbreviations

# GIT
abbr -a g git
abbr -a gst git status
abbr -a gcm git commit -m
abbr -a lg git log --graph --pretty=oneline --abbrev-commit --decorate
abbr -a glg git log-detail
abbr -a glb git log-branch
abbr -a glgs git log-summary
abbr -a ga git add
abbr -a gco git checkout
abbr -a gcb git checkout -b
abbr -a gd git diff
abbr -a gdl 'git diff --color=always | less -r'
abbr -a gap git add -p 
abbr -a gp git push
abbr -a gl git log
abbr -a gm git merge
abbr -a gcl git clone

# Other
abbr -a cl clear

# export LC_ALL=en_US.UTF-8  
# export LANG=en_US.UTF-8
